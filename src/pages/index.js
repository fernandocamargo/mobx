import { lazy } from "react";

export const HomePage = lazy(() => import("./HomePage"));

export const ModelerPage = lazy(() => import("./ModelerPage"));

export const FrameworksPage = lazy(() => import("./FrameworksPage"));
